# The live config names project keys; this is a public repo.
config.local.yml

# `go build` with no -o drops the binary here, named after the package.
/jira-dash

# `go build ./cmd/jira` with no -o drops the binary here, named after the package.
/jira
